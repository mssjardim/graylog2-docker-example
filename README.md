# graylog2-docker-example

# Install Docker
https://docs.docker.com/install

# Config
```shell
# cp .env.default .env
# vi .env

replace <external ip> with a valid IP address
save and exit .env file
```

# Running
```shell
# ./deploy.sh
```
